  <div class="activities">
    <div class="container-activities">
      <div class="banner-activities">
         <h1>Être avec toi, pour trouver la personne de confiance</h1>
          <%= render 'shared/searchbaractivities' %>
      </div>

      <div class="info-activities">
        <p>Parallel, c'est donner un accès simplifé à des personnes de confiance<br> pour vous accompagner  dans la découverte d'une nouvelle pratique.</p>
      </div>
      <div class="info-activities-2">
        <h5>Des questions? Prends le temps, là pour toi de découvrir.</h5>
      </div>

      <div class=boutons-activities>
        <% i = 1 %>
        <% @activities.each do |activity| %>
          <%= link_to activity.name, activity_path(activity), class: "btn-activities btn-#{i}"  %>
          <% i += 1 %>
        <% end %>
      </div>
    </div>
  </div>
